(ns voting-server.styles
	(:gen-class)
)

;  css

(def body "background-color: LightSkyBlue;")

(def h1 "text-align: center; font-size: xx-large; padding: 20px;")

(def h2 "text-align: center; font-size: x-large;")

(def outer "width: 1000px; padding:10px; margin-left: auto; margin-right: auto;")

(def container "width: 700px; padding: 10px; margin-left: auto; margin-right: auto; border: solid 5px blue")

(def large-print "font-size: xx-large")

(def para "padding: 5px;")

(def login-group "padding: 5px; display: grid; grid-template-columns: 1fr 3fr; grid-template-rows: repeat(1, auto);")

(def register-group "padding: 5px; display: grid; grid-template-columns: 1fr 3fr; grid-template-rows: repeat(2, auto);")
